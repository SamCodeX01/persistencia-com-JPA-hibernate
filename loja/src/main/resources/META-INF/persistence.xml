<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
                                 http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

<!-- O objetivo da tag <persistence-unit> no arquivo persistence.xml? É agrupar as configurações de uma unidade de persistência, que representa um banco de dados utilizado pela aplicação-->
    <persistence-unit name="loja">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Opcional: lista de classes entidade (se não usar scanning automático) -->
<!--         <class>com.exemplo.Usuario</class> -->
         <class>br.alura.loja.modelo.Produto</class>


        <properties>
            <!-- Configuração de conexão H2 -->
            <property name="jakarta.persistence.jdbc.driver" value="org.h2.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:h2:mem:loja;DB_CLOSE_DELAY=-1"/>
            <property name="jakarta.persistence.jdbc.user" value="sa"/>
            <property name="jakarta.persistence.jdbc.password" value=""/>

            <!-- Dialeto do H2 -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.H2Dialect"/>

            <!-- Comportamento do schema -->
            <property name="hibernate.hbm2ddl.auto" value="create-drop"/>

            <!-- Configurações opcionais úteis -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.hbm2dll.auto" value="create"/>

            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.use_sql_comments" value="true"/>

            <!-- Para ver o console web do H2 (opcional) -->
            <!-- <property name="hibernate.hikari.dataSource.url"
                      value="jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1"/> -->
        </properties>
    </persistence-unit>

</persistence>